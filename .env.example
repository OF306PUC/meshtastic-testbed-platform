# Copy to .env and fill in. .env is gitignored and holds local secrets.

# LoRa channel pre-shared key (base64). Must be identical on every node and the
# gateway. Generate/inspect with the Meshtastic CLI or app; format: base64:...
LORA_CHANNEL_PSK=base64:CHANGE_ME
