# Dependency set for the proxy-site edge collector.
#
# Deliberately smaller than src/gateway/requirements.txt: the collector reads
# the node console as TEXT and never speaks the Meshtastic protocol, so the
# `meshtastic` package and its protobuf stack are not needed here.
paho-mqtt==2.1.0
pyserial==3.5
