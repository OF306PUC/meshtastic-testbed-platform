[agent]
  interval = "1s"
  round_interval = true
  metric_batch_size = 1000
  metric_buffer_limit = 10000
  collection_jitter = "0s"
  flush_interval = "10s"
  flush_jitter = "0s"
  precision = ""
  hostname = ""
  omit_hostname = false

[[inputs.mqtt_consumer]]
  servers = ["tcp://mosquitto:1883"]
  topics = [
    "lora-testbed/+/position",
    "lora-testbed/+/device",
    "lora-testbed/+/environment"
  ]
  qos = 0
  connection_timeout = "30s"
  data_format = "json"
  json_time_key    = "received_at"
  json_time_format = "unix"
  tag_keys = ["node_id", "node_label", "topic"]

[[outputs.influxdb]]
  urls = ["http://influxdb:8086"]
  database = "cpsrtc_lora_telemetry"
  # Credentials interpolated from the telegraf container's environment
  # (injected via env_file: configuration.env in docker-compose.yaml).
  username = "${INFLUXDB_ADMIN_USER}"
  password = "${INFLUXDB_ADMIN_PASSWORD}"
  retention_policy = ""
  write_consistency = "any"
  timeout = "5s"